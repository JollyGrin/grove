# Recording outputs are regenerated by tapes/run.sh — not repo state.
# Exception: sample GIFs may be committed ON A PR BRANCH ONLY (git add -f)
# so reviewers can watch them via the branch raw URL; remove before merge.
*
!.gitignore
