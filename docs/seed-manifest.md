# Seed manifest — what was taken from overstory, and what must change

> **Source:** `overstory-tui` @ `8c2f4f059e841f063e718efd079681f92323228e`
> (`8c2f4f0 add doc for grove`), copied 2026-07-03.
> **Rule:** copied packages stay **byte-comparable with upstream** (only
> the import path `github.com/JollyGrin/overstory-tui` →
> `github.com/JollyGrin/grove` differs) until a plan task deliberately
> generalizes them. Record every deliberate divergence in the table at the
> bottom of this file. When a copied package confuses you, `diff -r`
> against upstream first.

## Copied byte-identical (imports rewritten only)

| What | Notes |
|---|---|
| `internal/tmux` · `git` · `worktree` · `detect` · `transcript` | Twice-proven (parkranger → ovs → grove). Upstream additions live in each package's separate `ovs.go` file — keep that convention (rename to `grove.go` only when generalizing). |
| `internal/state` | Event log + **mail model** (there is no separate mail package — design review I-7) + derived tasks.json. |
| `internal/cost` · `audit` · `hooks` · `tui` · `github` | Provider-neutral as-is. |
| `internal/config` · `doctor` · `kickoff` · `linear` | Copied verbatim but **destined for generalization** (see below). |
| `cmd/ovs/main.go` → `cmd/gv/main.go` | Thin glue; verb table intact. |
| `orchestrator/` (CLAUDE.md + embed.go) | The Grid-flavored brain; becomes generic-plus-pack-overlay in Phase 4. |
| `prompts/` (empty) · `config.example.yaml` · `go.mod`/`go.sum` (module renamed) · `.gitignore` | — |

**Verification at seed time:** `go build ./...`, `go vet ./...`,
`go test ./...` all green; `gofmt -l .` empty.

## NOT copied (deliberately)

- `.env` (secrets stay behind env-var indirection).
- ovs's `DESIGN.md` / `TASKS.md` / `LEARNINGS.md` / `ONBOARDING.md` /
  `docs/plans/*` — grove has its own (LEARNINGS.md seeded with the generic
  subset; the Grid-specific entries belong in the future Grid pack's L5
  layer, per the interview decisions).

## The generalization map (Phase-0+ work, under plan review)

| Surface | Today (as copied) | Target |
|---|---|---|
| **P0.0 — runtime namespaces (BLOCKS running the binary)** | config `~/.config/overstory/` · state `~/.local/state/overstory/` · env `OVERSTORY_STATE_DIR` · hook commands `ovs hook <event>` + absolute `~/go/bin/ovs` · tmux session names (`ovs`, `ovs-mobile`) · notifier titles | `~/.config/grove/` · per-workspace `.grove/state/` (DESIGN.md §6.5.1; global dir is an acceptable P0.0 interim) · `GROVE_STATE_DIR` · `gv hook` + `~/go/bin/gv` · `grove-<label>` |
| `internal/config` | Hard `linear:` struct, global single config, `ccwork` default | Layered config (connections-design §7): flags > `config.local.yaml` > `.grove/config.yaml` > pack > user > defaults; `provider:` section |
| `internal/linear` | Directly-called client | `TaskProvider` adapter behind the interface (DESIGN.md §5); `markdown` provider is new and comes first |
| `internal/doctor` | Grid checks hard-coded (ccwork plugins, universal CLAUDE.md symlink, LINEAR_API_KEY, `zsh -ic` alias probe) | Derived from the connections manifest (connections-design §3–5); Grid checks move to the Grid pack; probe `$SHELL`, not hardcoded zsh |
| `internal/kickoff` | Three Grid/Linear monolithic templates | Assembly: frame + provider verbs + pack fragments + learnings block + sentinel (connections-design §6.2). Sentinel stays core and non-negotiable |
| `orchestrator/CLAUDE.md` | Grid duties, operator referenced directly, dev-linear MCP | Generic duties skeleton + pack overlay, rendered composed + hash-tracked (connections-design §6.3) |
| `internal/hooks` | Installs into `~/.cc-work/settings.json`; "not my session" = untracked cwd | Same mechanism, gv-namespaced; "not my session" = **cwd not a task in this workspace's tasks.json** (dual-hook contract, DESIGN.md §12) |
| macOS-isms | `terminal-notifier` required in doctor | Desktop notification as a connection with per-OS fixes; Linux = tested tier 2 |

## Dummy-data E2E (the recipe that must not be lost)

Exercise the full grab/ls/untrack/audit/adopt loop with zero risk to any
live state: point `HOME` at a scratch dir (isolates config), set the state
env override (`OVERSTORY_STATE_DIR` today, `GROVE_STATE_DIR` after P0.0)
at a scratch dir, use a scratch git repo with a dummy config whose repo
`claude:` command is a harmless `echo`. The worker "session" echoes and
exits; every state transition, hook path, and cleanup verb is still
exercised for real. This is the Phase-0 acceptance harness (DESIGN.md
§13 Phase 0) and the pattern for all future E2E tests.

## Deliberate divergences from upstream (append-only log)

| Date | Package/file | Divergence | Plan task |
|---|---|---|---|
| 2026-07-03 | all | Module path `JollyGrin/overstory-tui` → `JollyGrin/grove`; `cmd/ovs/` → `cmd/gv/` | seed |
| 2026-07-04 | `internal/config` | P0.0 paths (`~/.config/grove`, `~/.local/state/grove`, `GROVE_STATE_DIR`) + new `provider:` section (kind default `markdown`, `markdown.dir`) | phase-0 T1/T2 |
| 2026-07-04 | `internal/hooks` | gv notifier group/titles; installer predicate rewritten to basename matching (`isGvEntry`) so shared-settings ovs entries are never claimed; `install`/`installed` made path-injectable for tests | phase-0 T1 |
| 2026-07-04 | `internal/transcript/session.go` | `OVS_CLAUDE_CONFIG_DIR` → `GV_CLAUDE_CONFIG_DIR` (parse/session logic untouched) | phase-0 T1 |
| 2026-07-04 | `internal/audit` · `internal/tui` · `internal/doctor` · `cmd/gv` · `orchestrator/CLAUDE.md` · `config.example.yaml` | Mechanical ovs→gv/grove rename of command strings, banners, hints | phase-0 T1 |
| 2026-07-04 | `internal/tmux/ovs.go` | Relay buffer `ovs-relay` → `gv-relay` (tmux buffers are server-global; avoids clobbering live ovs relays) | phase-0 T1 |
| 2026-07-04 | `internal/kickoff` | Generalized: renders `provider.Task` + `Verbs`, per-provider template sets. Linear output **byte-identical** (goldens in `testdata/`); `{{.Identifier}}` alias kept for repo overrides; new generic `md_*.tmpl` set. Linear templates keep the Review sentence literal (its multi-line wrap defeats clean verb slotting) — full verb assembly is Phase 4 | phase-0 T3 |
| 2026-07-04 | `internal/provider` (new) | TaskProvider seam — **P0 read subset of DESIGN §5.1**: no `Attach`; `Capabilities{CanList}` only (`canTransition`/`canComment`/`autoLinksPR` deferred until a write path needs them) | phase-0 T2 |
| 2026-07-04 | `internal/git/grove.go` (new file) | `HasRemote` + `BaseRef` (no-remote grab fallback); upstream files untouched | phase-0 T3 |
| 2026-07-04 | `internal/bootstrap` (new) | P0 `gv init` scaffold (register repo, `.grove/tasks/`, sample task) | phase-0 T5 |
| 2026-07-04 | `cmd/gv/main.go` | grab/adopt/findTask/done rewired through the provider; no-arg grab lists the backlog; done no-remote degraded path (`--force` = human confirmation) | phase-0 T3/T4 |
| 2026-07-04 | `internal/state/grove.go` (new file) | `ReadEvents` (activity-feed read side); state.go untouched | cockpit-lite |
| 2026-07-04 | `internal/tmux/grove.go` (new file) | `MainVertical` + `SpawnPane` (cockpit layout / orchestrator-pane spawn); upstream files untouched | cockpit-lite |
| 2026-07-04 | `internal/tui` | MAIL/REVIEW panels removed for the ACTIVITY feed (cockpit design §2/§5); single-panel selection; `O` spawn keybind via injected hook | cockpit-lite |
| 2026-07-04 | `internal/config` · `cmd/gv/main.go` | Orchestrator default `ccwork` → `claude --dangerously-skip-permissions` (DESIGN §15 locked decision); bare `gv` = cockpit, `gv dash` = TUI-only; cockpit pane runs the binary by absolute path | cockpit-lite |
| 2026-07-04 | `internal/probe` · `internal/connections` · `internal/wizard` (new) | Phase 1a: deterministic probe, connections manifest (grid-interim instances isolated in `grid_interim.go` for the 1b pack lift), wizard step-builder | phase-1a T1/T2/T6 |
| 2026-07-04 | `internal/doctor` | Rewritten as a renderer of `connections.EvaluateAll`. Three deliberate behavior changes: terminal-notifier hard→warn (darwin-only), LINEAR_API_KEY row conditional on `provider.kind==linear`, dev-linear MCP reminder → static grid-interim warn; exit code now errors-only (warnings pass) | phase-1a T3 |
| 2026-07-04 | `internal/bootstrap` | +`writer.go` (comment-preserving field-merge Doc; no-op runs never rewrite) · +`agentsmd.go` (templated one-shot AGENTS.md agent) · exported `ScaffoldTasks` | phase-1a T4/T5 |
| 2026-07-04 | `cmd/gv/main.go` · `go.mod` | cmdInit → interactive wizard (huh forms; +deps charmbracelet/huh, golang.org/x/term); cmdDoctor --json | phase-1a T6 |
| 2026-07-04 | `cmd/gv/main.go` | Orchestrator panes launch with `--add-dir` for every configured repo + existing worktrees dir — cwd stays the orchestrator dir (brain reloads on /clear, hook-invisible) but @-references/file tools reach the fleet | orch-adddir |
| 2026-07-04 | `internal/config` · `internal/provider` · `cmd/gv/main.go` | Per-repo `provider:` override (mixed Linear+markdown fleets until per-workspace config); `FromConfigKind`; id resolution via `IDCandidates` (both id shapes live at once — tracked state is the arbiter) | grid-config |
| 2026-07-04 | `internal/hooks` · `internal/connections` · `internal/wizard` · `cmd/gv` | Hooks install per worker PROFILE: `SettingsPaths` derives each settings.json from the configured worker commands (`ccwork`→~/.cc-work, plain claude→~/.claude, `CLAUDE_CONFIG_DIR=`→that dir); doctor shows one row per profile; wizard names the real paths. Fixes personal-profile workers being hook-invisible | hooks-profiles |
| 2026-07-05 | `internal/workspace` (new) · `internal/config` · `internal/state/grove.go` · `internal/hooks` · `internal/tui` · `cmd/gv` | Workspaces: nearest-`.grove/` ambient resolution, registry (flock), yaml-level config merge (workspace over global; repos/provider wholesale), per-root state, TUI shows `GROVE · <label>`, hook receiver scans candidates read-only (workspaces sorted, legacy last), `grove-<label>` cockpits with per-workspace orchestrator dir, `gv switch`/`gv workspaces`, parent-scope `gv init`. NOTE: any repo with `.grove/` (incl. a P0 `.grove/tasks/`) is now an implicit workspace root | workspaces W1-W8 |
| 2026-07-05 | `internal/provider` (+github.go) · `internal/connections` · `internal/wizard` · `cmd/gv` | github-issues provider (gh CLI, injectable runner, 15s timeout, cap-visible List); cmdGrab non-linear branch generalized to the repo's effective kind (was hardcoded markdown); findTask short-ref numeric-suffix fallback; providerConnections iterates per-repo effective kind | github-issues T1/T2 |
| 2026-07-07 | `internal/transcript/{parse_test,session_test}.go` · `internal/tui/feed_test.go` | OSS pre-publish scrub: personal username/repo path fixtures genericized and an operator reference de-named. Test logic and assertions unchanged (fixture values are arbitrary; input/expected pairs kept consistent) | oss-scrub |
| 2026-07-07 | `internal/transcript/session.go` · `session_test.go` | `ProjectDir` fallback `~/.cc-work` → `~/.claude` (grove workers run under the default Claude Code config dir; `~/.cc-work` is a work-subscription profile grove must never read by default — see CLAUDE.md coexistence rule). `GV_CLAUDE_CONFIG_DIR` override kept (ovs-style repos + e2e). e2e fixture path moved to `~/.claude/projects/` accordingly | grove-16 |
| 2026-07-13 | `internal/schema` (new) · `internal/state/state.go` · `internal/doctor` · `cmd/gv/main.go` | Surface-plugin contract v1 (docs/plugins.md): `Event.V` stamp (`"v":1`, stamped in `Append`; unstamped records read as v1 via `Event.Version()` in grove.go) and every `--json` payload wrapped in the `{schema_version, <key>: payload}` envelope (`schema.Envelope`); doctor `RenderJSON` emits the envelope | grove-75 |
| 2026-07-13 | `internal/tmux/tmux.go` · `internal/tmux/ovs.go` | Every session-scoped `-t` target `=`-anchored via `Exact()` (grove.go): tmux resolves a bare `-t <session>` against window names across ALL sessions, so a session literally named `grove` collided with `grove · <ticket>` worker windows (live: `new-window -t grove` → "index 1 in use"). Window side of `session:window` left prefix-tolerant (glyphs). ovs.go: `ClaudePane` session side anchored | grove-78 |
| 2026-07-17 | `internal/state/state.go` · `internal/audit` · `internal/tui` · `cmd/gv/main.go` | `gv pause` (grove-90): additive `task_paused` event + `Task.Paused` fold (cleared by created/adopted/session_started; pause normalizes agent to idle), Label/SortRank paused branch; audit class `paused` (precedence merged > drifted > paused > abandoned/disconnected, suggestion `gv adopt`); ls ⏸ status, cockpit ⏸ glyph, paused rows skip pane scrape + leave MAIL. Plugin contract: additive event type + class value only | grove-90 |
| 2026-07-17 | `internal/tmux/tmux.go` | grove-78 correction: `SplitVertical` (and every grove.go helper whose `-t` is a target-pane/window — `set-option`, `show-options`, `select-layout`, `split-window`) re-anchored via `ExactActive()` (`=name:`): tmux 3.6a rejects bare `=name` for those commands ("no such session"), which broke every cockpit build. `Exact()` stays for true target-session commands | grove-99 |
| 2026-07-18 | `internal/tmux/tmux.go` · `internal/tmux/ovs.go` | Worker-window targets resolved by id, never by name (grove-116): the window side of `session:name` also prefix-matches, and worker names are prefixes of each other (`repo · grove-1` vs `repo · grove-10`), so once a task's own window died its target silently resolved to the sibling — KillWindow killed a live worker mid-turn, ClaudePane pasted answers into the wrong agent. tmux.go `KillWindow`/`AttachWindow` and ovs.go `ClaudePane` now resolve via `WindowID` (grove.go: list-windows + `matchesWindowName` → `@N`; glyph tolerance preserved, sibling unhittable) and error/fall back instead of touching a sibling | grove-116 |
| 2026-07-29 | `internal/tmux/ovs.go` | `PasteText` becomes deliver-**and-verify** (grove-144): bracketed paste (`paste-buffer -d -p` — the doc comment had claimed bracketed paste while the flag was missing), a 250ms settle before the Enter, then a submit check that scrapes the pane's input box (`inputBoxContent`/`pasteLanded`), retries the Enter once and returns an actionable error instead of nil when the text is still unsent — callers must not record `EvAnswered` on that error. Zero-settle Enter was being swallowed by a TUI mid-paste, so relays landed as unsent `[Pasted text]` while gv reported success. Scrape reads the whole visible pane, never `CapturePaneBottom` (bottom-N-rows is blank when the app draws from the top). `SendRawKey` unchanged | grove-144 |
| 2026-07-29 | `internal/tmux/grove.go` · `internal/detect/live.go` · `internal/tui/tui.go` | Dash 1s refresh batched to O(1) tmux spawns (grove-149): the tick ran 3× `list-windows` + `list-panes` + `display-message` per task per second (6N+2 processes/sec — pegged CPUs where process spawn is expensive: EDR scanning each exec, WSL1). New `SessionSnapshot` (grove.go) answers ActiveWindow / ResolveWindowName / WindowID / WindowExists / ClaudePane for the whole session from ONE `list-windows` + ONE `list-panes -s` (glyph tolerance and grove-116 sibling rules preserved via `matchesWindowName`/`pickPane`; `-t` targets `=`-anchored — both commands are target-session per the grove-99 table); `CapturePaneBottomKnown` captures with the snapshot's pane height, deleting the per-capture `display-message` query. detect/live.go gains `DetectLiveFrom(snapshot, base)` — `classifyPaneOutput` untouched (byte-comparable probe core); stateless `DetectLive` stays for one-shot callers (`gv ls`). `refreshCmd` memoizes one snapshot per session per tick: 2 session-wide reads + 1 capture per live task (≈N+3 spawns/sec, was 6N+2) | grove-149 |
| 2026-07-29 | `internal/state/folder.go` (new) · `internal/hooks/hooks.go` · `internal/tui/tui.go` | Cockpit state I/O made O(new events) per tick (grove-126, the other half of grove-149's hot path): new `state.Folder` (new file — `state.go` stays byte-comparable) remembers the consumed events.jsonl byte offset + running fold state, so each 1s refresh parses only appended bytes and answers BOTH the task map and the 200-event feed tail from one pass (was `Load` + `ReadEvents`, two full-log scans/sec, forever-growing). Torn appends (no trailing `\n`) held back until complete; complete-but-malformed lines skipped like `ReadEvents` (deliberate divergence from `Load`, which stops folding at the first scar); shrunk/missing log resets + refolds; returned tasks are per-call copies (render loop never races the fold). tasks.json rewritten only when the marshaled view's fnv-64 hash changes (shrinks the #121 write-race window; atomic rename stays #121). `prsCmd` nil-fallback: full `Load` → read-only `ReadTasks`. hooks.go: stop events cap stored `data["message"]` at 2000 runes (head, rune-safe; classification still on full text — reader-side truncation stays #123). CLI/almanac/audit keep cold `Load`/`ReadEvents` | grove-126 |
| 2026-08-20 | `internal/tmux/ovs.go` · `internal/tmux/grove.go` · `cmd/gv/main.go` | Pane targeting made `pane-base-index`-proof (grove-168): `SplitVerticalWindow` now returns the new pane's `%N` id (`split-window -P -F '#{pane_id}'`) and grab/adopt send the claude command to that id instead of a literal `.1`; new `FirstPaneID(windowTarget)` (grove.go) resolves a window's lowest-index pane for the cockpit hint / dash launch / nvim inject / `gv mobile`, replacing literal `.0` targets; `closablePane` guards "the window's lowest-index pane", not `index == 0`. `ClaudePane`'s literal-1 fallback kept (fires only when the window is unlistable — behavior-preserving). ovs upstream keeps the old void `SplitVerticalWindow`; backport candidate | grove-168 |
| 2026-08-17 | `internal/audit/process.go` · `internal/audit/process_gather.go` · `internal/audit/gather.go` · `cmd/gv/main.go` | Worktree-process reaping (grove-156): teardown (`gv done` / `untrack --rm`) SIGTERMs every process whose argv references the task's grove-created worktree path immediately before worktree removal (build/test children — jest-worker — daemonize out of the pane and survive `tmux kill-window`; SIGTERM-only, 2s wait, survivors reported, never SIGKILL). New pure `DetectWorktreeProcesses` + `WorktreeProcess`; audit/sweep gain a `worktree_processes` class for processes referencing a tracked worktree whose task is done or whose dir is gone (sweep offers per-item confirmed SIGTERM). Hard scoping: only `worktree` paths from tasks.json rows are ever matched — never a generic `.worktrees/` pattern. ps scan gains `rss`; `orphan_processes` rows gain additive `rss_kb`. Plugin contract: additive fields only | grove-156 |
| 2026-08-20 | `internal/state/state.go` | `Load` now skip-and-continues past a complete-but-malformed events.jsonl line, matching `Folder.consume` (grove-166): the prior break-on-first-error behavior (kept for ovs byte-comparability) meant one crash-torn line buried mid-file silently dropped every later event for every CLI command, and the tasks.json rewrite that follows persisted the truncated fold. Line-oriented read (`bufio.Reader.ReadBytes('\n')` + per-line `json.Unmarshal`, mirroring `Folder.consume`) replaces the whole-file `json.Decoder`, since a Decoder's position after a decode error doesn't reliably resync to the next line boundary. `EventCounts` untouched (out of scope) | grove-166 |
| 2026-08-22 | `internal/state/state.go` · `cmd/gv/main.go` | `gv handoff` (grove-177): additive `task_handed_off` event + `Task.HandedOffTo` fold (Done=true; SessionID/Agent/Sentinel/Question cleared; cleared by created/adopted, and by `task_untracked` — the tombstone's terminal path), `state.HandedOff`, read-only `state.Peek` (Load minus the tasks.json rewrite, for poll loops); `gv ls` appends tombstone rows (`handed_off_to`, `live: handed-off`) after the active rows; `gv adopt --sync` (fetch + hard-reset to origin/<branch> — handoff pickups where another host worked the branch); `gv untrack` accepts tombstone rows. Plugin contract: additive event type + field only | grove-177 |
| 2026-08-22 | `internal/github/github.go` | `OpenPRBody(repoDir, branch)` — number/url/body of the open PR for a head branch via `gh pr list --json` (grove-177 handoff verify: the PR body is the handoff carrier). Additive; upstream untouched | grove-177 |
| 2026-08-22 | `internal/config/config.go` | `hosts:` map (`ssh:`/`gv:` per host, GV default `"gv"`, field-wise layer merge) + `Config.Host`/`HostNames` — grove-176 remote passthrough (landed with #181; row recorded during the grove-177 fix round). Additive section, no upstream counterpart | grove-176 |
| 2026-08-29 | `internal/state/state.go` · `internal/watch` (new) · `cmd/gv/watch.go` (new) | `gv watch` (grove-205): additive `Task.SentinelAt` (`sentinel_at`, a `*time.Time` so `omitempty` can actually omit it) stamped by the `agent_status` fold and cleared by adopt/handoff and by the sentinel `"none"` (the absence of a sentinel, not one of its own) — `updated` moves for any event, so a poll-based consumer had no way to date the CURRENT sentinel. New `internal/watch`: a read-only events.jsonl follower (byte-offset tail, torn trailing line held back, malformed line skipped, replaced log re-followed) with a from-now default baseline taken at construction, plus filtering (ticket/type/sentinel/since) and the human row renderer. Deliberately NOT `state.Folder`, which folds and rewrites the derived tasks.json — watch is pure read. Plugin contract: additive field + a new read command | grove-205 |
