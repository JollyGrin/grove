workspace:
    label: grove-repo
    scope: repo
repos:
    grove:
        path: /Users/grins/git/grove
        base: main
        claude: claude --dangerously-skip-permissions
provider:
    kind: github
